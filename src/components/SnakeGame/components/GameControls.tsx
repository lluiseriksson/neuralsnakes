
import React from 'react';

// GameControls is now empty as we're removing the manual evolution button
// The game will now evolve automatically based on neural network performance
const GameControls: React.FC = () => {
  return null; // Return null since we're removing this component
};

export default GameControls;
